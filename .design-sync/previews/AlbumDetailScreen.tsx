import { AlbumDetailScreen } from '@lyrebird/design-system'

/** Full Album Detail window — Sidebar + hero header + numbered track list + pinned PlayerBar. */
export const Window = () => (
	<div style={{ width: 1192, height: 752 }}>
		<AlbumDetailScreen />
	</div>
)
