import { ArtistDetailScreen } from '@lyrebird/design-system'

/** Full Artist Detail window — Sidebar + artist header + Top Songs + Discography
 *  + Similar Artists shelves + pinned PlayerBar. */
export const Window = () => (
	<div style={{ width: 1192, height: 752 }}>
		<ArtistDetailScreen />
	</div>
)
