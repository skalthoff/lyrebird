import { LibraryScreen } from '@lyrebird/design-system'

/** The full Library window — Sidebar + search toolbar + album grid + pinned PlayerBar. */
export const Window = () => (
	<div style={{ width: 1192, height: 752 }}>
		<LibraryScreen />
	</div>
)
