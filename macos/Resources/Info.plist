<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Info.plist template for Lyrebird.app.

  Do NOT edit $VERSION / $BUILD in place. `macos/Scripts/make-bundle.sh`
  copies this file into the bundle and then uses `plutil -replace` to fill
  in CFBundleShortVersionString / CFBundleVersion from the env vars
  $VERSION and $BUILD (or, when unset, from `git describe` and the commit
  count). Keep those string literals unique so `plutil -replace` targets
  the right keys if anything else ever needs similar substitution.

  Keys intentionally omitted:
    - LSUIElement: Lyrebird is a Dock-visible app.
    - NSMicrophoneUsageDescription / NSCameraUsageDescription / similar
      TCC usage strings: we do not touch those APIs yet. Apple's notary
      rejects unused purpose strings on newer tool versions.
    - App Sandbox keys: we are NOT targeting the Mac App Store. Hardened
      runtime + notarization is sufficient for Developer ID distribution.
-->
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key>
    <string>Lyrebird</string>
    <key>CFBundleIdentifier</key>
    <string>org.lyrebird.desktop</string>
    <key>CFBundleName</key>
    <string>Lyrebird</string>
    <key>CFBundleDisplayName</key>
    <string>Lyrebird</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>

    <!-- Version fields. These placeholders are overwritten by
         make-bundle.sh via `plutil -replace` at build time. -->
    <key>CFBundleShortVersionString</key>
    <string>$VERSION</string>
    <key>CFBundleVersion</key>
    <string>$BUILD</string>

    <key>CFBundleIconFile</key>
    <string>AppIcon</string>

    <key>LSMinimumSystemVersion</key>
    <string>14.0</string>
    <!--
        Classifies the app as a music player. macOS uses this for App Store
        categorization and (more relevantly here) to mark the app as a media
        producer, which is what MPNowPlayingInfoCenter / Control Center
        expect. AVAudioSession is iOS-only — background playback on macOS is
        automatic for a regular GUI app as long as it doesn't set
        LSBackgroundOnly or LSUIElement. See issue #47.
    -->
    <key>LSApplicationCategoryType</key>
    <string>public.app-category.music</string>

    <key>NSHighResolutionCapable</key>
    <true/>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
    <key>NSHumanReadableCopyright</key>
    <string>© 2026 Lyrebird. GPL-3.0-only.</string>
    <key>NSSupportsAutomaticTermination</key>
    <true/>
    <key>NSSupportsSuddenTermination</key>
    <true/>

    <!-- Custom URL scheme (lyrebird://) for Jellyfin OAuth / quick-connect callbacks (org.lyrebird.desktop / lyrebird://).
         The Rust core receives these via the AppDelegate URL handler. -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleTypeRole</key>
            <string>Viewer</string>
            <key>CFBundleURLName</key>
            <string>org.lyrebird.desktop</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>lyrebird</string>
            </array>
        </dict>
    </array>

    <!--
        Sparkle 2 auto-update configuration. SUFeedURL points at the
        appcast.xml hosted on GitHub Pages (docs/ → gh-pages). SUPublicEDKey
        is the base64-encoded Ed25519 public half of the signing keypair;
        the private half lives in the SPARKLE_ED25519_PRIVATE GitHub secret
        and only touches the release runner. Rotating either value is a
        release-breaking change — bump the feed URL with a new filename so
        older installs keep talking to the old URL until they update.

        SUPublicEDKey below is the placeholder that make-bundle.sh / the
        release workflow replaces with the real key at build time. Never
        commit the real key here: Info.plist is world-readable but the
        check-in history is what matters for key rotation hygiene.
    -->
    <key>SUFeedURL</key>
    <string>https://skalthoff.github.io/lyrebird-desktop/appcast.xml</string>
    <key>SUPublicEDKey</key>
    <string>@@SPARKLE_PUBLIC_ED_KEY@@</string>
    <!--
        Enforce Sparkle's strict Ed25519 verification. Without this, Sparkle
        will accept either a DSA-signed or Ed25519-signed feed; we only
        ever publish Ed25519 so locking it down rejects downgrade attacks.
    -->
    <key>SUEnableAutomaticChecks</key>
    <true/>
    <key>SUScheduledCheckInterval</key>
    <integer>86400</integer>

    <!--
        Sentry crash reporting DSN (#442). This key is intentionally EMPTY
        in the repository — it must never contain a real DSN. The release
        workflow substitutes the real value at build time via:
            plutil -replace LyrebirdSentryDSN -string "$SENTRY_DSN" Info.plist
        When absent or empty, CrashReporter.isAvailable returns false and
        Sentry is a guaranteed no-op even if the user toggles the preference.
        Developers can set a local override via:
            defaults write org.lyrebird.desktop sentry.dsnOverride "https://…@…"
    -->
    <key>LyrebirdSentryDSN</key>
    <string></string>
</dict>
</plist>
